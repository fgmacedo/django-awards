============
Installation
============

At the command line::

    $ easy_install django-awards

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-awards
    $ pip install django-awards
